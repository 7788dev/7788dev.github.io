<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <author>
    <name>Looks</name>
  </author>
  <generator uri="https://hexo.io/">Hexo</generator>
  <id>https://7788dev.github.io/</id>
  <link href="https://7788dev.github.io/" rel="alternate"/>
  <link href="https://7788dev.github.io/atom.xml" rel="self"/>
  <rights>All rights reserved 2026, Looks</rights>
  <subtitle>属于我的思考</subtitle>
  <title>Looks Blog</title>
  <updated>2026-05-08T14:05:31.117Z</updated>
  <entry>
    <author>
      <name>Looks</name>
    </author>
    <category term="逆向" scheme="https://7788dev.github.io/categories/%E9%80%86%E5%90%91/"/>
    <category term="逆向" scheme="https://7788dev.github.io/tags/%E9%80%86%E5%90%91/"/>
    <category term="Cloudflare" scheme="https://7788dev.github.io/tags/Cloudflare/"/>
    <category term="AST" scheme="https://7788dev.github.io/tags/AST/"/>
    <category term="Node.js" scheme="https://7788dev.github.io/tags/Node-js/"/>
    <content>
      <![CDATA[<blockquote><p>本文只记录方法论与接口形态，不提供任何可直接运行的绕过实现。具体字段的加密算法、VM 字节码映射、签名逻辑一律略过。</p></blockquote><p>目标：在 Node 里端到端模拟 Cloudflare Managed Challenge 的挑战流程，拿到 <code>cf_clearance</code> 后用纯 HTTP 走业务接口。用到的栈是 <code>node-tls-client</code> + <code>node:vm</code> + <code>@babel/parser</code> + <code>piscina</code>。做了将近一个月，最终没过，原因放在最后。</p><h2 id="链路总览"><a href="#链路总览" class="headerlink" title="链路总览"></a>链路总览</h2><p>访问一个被 5s 盾保护的页面，浏览器要打三条核心接口才能拿到通行证：</p><figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs awk"><span class="hljs-number">1</span>. GET  /&lt;original&gt;               → 服务器返回一个 interstitial HTML<br>                                     + 内嵌 window._cf_chl_opt + 加载 orchestrate<br><span class="hljs-number">2</span>. GET  <span class="hljs-regexp">/cdn-cgi/</span>challenge-platform<span class="hljs-regexp">/h/</span>&#123;tier&#125;<span class="hljs-regexp">/orchestrate/</span>.../v1<br>                                   → 一段被混淆过的 challenge 主脚本<br><span class="hljs-number">3</span>. POST <span class="hljs-regexp">/cdn-cgi/</span>challenge-platform<span class="hljs-regexp">/h/</span>&#123;tier&#125;<span class="hljs-regexp">/flow/</span>ov1<span class="hljs-regexp">/.../</span>&lt;seed&gt;<br>                                   → VM 执行期间反复上报「当前环境快照」<br><span class="hljs-number">4</span>. POST <span class="hljs-regexp">/cdn-cgi/</span>challenge-platform<span class="hljs-regexp">/h/</span>&#123;tier&#125;<span class="hljs-regexp">/cv/</span>result<span class="hljs-regexp">/&lt;ray&gt;/</span>&lt;hash&gt;<br>                                   → 提交 Turnstile token 换 cf_clearance<br></code></pre></td></tr></table></figure><p>tier 是 <code>b</code>（bot-management）或 <code>g</code>（generic）；ray 是这次挑战的唯一 ID，从初始 HTML 的 <code>window._cf_chl_opt.cRay</code> 读；hash 是 orchestrate 路径里的最后一段，必须原样带回。</p><h2 id="三条接口的参数画像"><a href="#三条接口的参数画像" class="headerlink" title="三条接口的参数画像"></a>三条接口的参数画像</h2><h3 id="orchestrate（GET）"><a href="#orchestrate（GET）" class="headerlink" title="orchestrate（GET）"></a>orchestrate（GET）</h3><p>参数只有 URL 本身，关键在<strong>请求头</strong>和<strong>TLS 指纹</strong>。初始 HTML 里会注入一个 <code>&lt;script src=&quot;/cdn-cgi/challenge-platform/h/g/orchestrate/chl_api_b/v1?ray=...&quot;&gt;</code>，浏览器跟着请求。Cloudflare 在这一步已经开始做指纹了：</p><ul><li><code>sec-ch-ua</code> &#x2F; <code>sec-ch-ua-arch</code> &#x2F; <code>sec-ch-ua-bitness</code> &#x2F; <code>sec-ch-ua-full-version-list</code> 四件套要和声明的 UA 严格对得上</li><li><code>accept</code> 必须是 <code>*/*</code>，不是浏览器加载文档时那个长串</li><li><code>referer</code> 是初始页面</li><li>JA4 指纹必须是 Chrome（我用 <code>chrome_131</code> profile，对 Chrome 142 足够近似）</li><li>HTTP&#x2F;2 的 HEADERS 帧里伪 header 顺序固定：<code>:method :authority :scheme :path</code> 后才是真实 header</li></ul><p>少任何一条，服务端返回 403 + 一个假 challenge，后面所有步骤都不用做了。</p><h3 id="flow-ov-（POST）"><a href="#flow-ov-（POST）" class="headerlink" title="flow&#x2F;ov*（POST）"></a>flow&#x2F;ov*（POST）</h3><p>这是整个流程里<strong>交互最密集、参数最脏</strong>的一条。VM 执行过程中会往这里反复打点，单次挑战打 3~8 次。body 是一个 binary payload（byte array），content-type 是 <code>application/octet-stream</code>。</p><p>payload 的结构，粗略是：</p><figure class="highlight pgsql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs pgsql">[<span class="hljs-keyword">header</span> bytes | <span class="hljs-keyword">encrypted</span> body]<br></code></pre></td></tr></table></figure><p>header 里有 version、seq、rc（round count）这几个字段，encrypted body 是 VM 内部用 <code>_cf_chl_opt.cRay + flowSeed</code> 派生密钥加密过的一段结构化数据。数据内容大致分三类：</p><ul><li><strong>环境指纹</strong>：<code>navigator.userAgent / platform / hardwareConcurrency / languages</code>、<code>screen.width / height / colorDepth</code>、<code>document.documentElement.clientWidth</code>、<code>window.devicePixelRatio</code>、时区偏移、<code>performance.now()</code> 在各关键节点的读数</li><li><strong>DOM 证据</strong>：特定 id 元素的存在性、<code>innerText</code> hash、<code>getBoundingClientRect()</code> 返回值</li><li><strong>事件证据</strong>：<code>pointerover / pointermove / mousemove / click / focus / blur / touchstart</code> 的发生顺序、时间差、坐标</li><li><strong>VM 自检</strong>：对 <code>Function.prototype.toString</code>、<code>Error.stack</code>、<code>Object.getOwnPropertyNames(window)</code> 的回读</li></ul><p>response 是一段新的 JS，用 <code>new Function(body)</code> 执行，等价于”下一轮挑战指令”。也就是说 flow 是<strong>双向</strong>的——你上报环境，它回一段新的代码继续跑，类似一个远程调试器。</p><h3 id="cv-result（POST）"><a href="#cv-result（POST）" class="headerlink" title="cv&#x2F;result（POST）"></a>cv&#x2F;result（POST）</h3><p>最后一跳，拿 Turnstile token 换 <code>cf_clearance</code>。URL 形如：</p><figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs awk">POST <span class="hljs-regexp">/cdn-cgi/</span>challenge-platform<span class="hljs-regexp">/h/</span>&#123;tier&#125;<span class="hljs-regexp">/cv/</span>result<span class="hljs-regexp">/&#123;ray&#125;/</span>&#123;hash&#125;<br>Content-Type: application/x-www-form-urlencoded<br></code></pre></td></tr></table></figure><p>body 是 form-encoded：</p><table><thead><tr><th>字段</th><th>含义</th></tr></thead><tbody><tr><td><code>wp</code></td><td>Turnstile widget 回调里拿到的 token（<code>cf-turnstile-response</code>）</td></tr><tr><td><code>h</code></td><td>从 orchestrate 响应里提取的一个 opaque 字段</td></tr><tr><td><code>gv</code></td><td>挑战版本号，从 <code>_cf_chl_opt.cH</code> 取</td></tr><tr><td><code>cv</code></td><td>challenge variant，从 <code>_cf_chl_opt.bLtO6</code> 取</td></tr></tbody></table><p>返回 200 + <code>Set-Cookie: cf_clearance=...; HttpOnly; Secure; SameSite=None; Path=/</code>。后续带着这个 cookie 访问业务接口，直接通行。<strong>前提是前面所有指纹、flow 打点、token 生成都合规。</strong></p><h2 id="AST：从一堆-oK-kM-p-里找东西"><a href="#AST：从一堆-oK-kM-p-里找东西" class="headerlink" title="AST：从一堆 oK(kM.p) 里找东西"></a>AST：从一堆 <code>oK(kM.p)</code> 里找东西</h2><p>orchestrate.js 的混淆风格很典型：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">function</span> <span class="hljs-title function_">zq</span>(<span class="hljs-params">p,gD,gQ,oV,P,jM,...</span>) &#123;<br>  gD = &#123;<span class="hljs-attr">p</span>:<span class="hljs-number">283</span>,<span class="hljs-attr">P</span>:<span class="hljs-number">1072</span>,<span class="hljs-attr">jM</span>:<span class="hljs-number">590</span>,<span class="hljs-attr">jE</span>:<span class="hljs-number">1230</span>,...&#125;;   <span class="hljs-comment">// 数字字典</span><br>  oV = qY;                                   <span class="hljs-comment">// 字符串解码函数</span><br>  ...<br>  <span class="hljs-keyword">switch</span>(jE[jd++]) &#123;<br>    <span class="hljs-keyword">case</span> <span class="hljs-string">&#x27;0&#x27;</span>: jM[<span class="hljs-title function_">oV</span>(gD.<span class="hljs-property">p</span>)](p, <span class="hljs-title function_">oV</span>(gD.<span class="hljs-property">P</span>)) &amp;&amp; ...; <span class="hljs-keyword">continue</span>;<br>    <span class="hljs-keyword">case</span> <span class="hljs-string">&#x27;1&#x27;</span>: jd = s[<span class="hljs-title function_">oV</span>(gD.<span class="hljs-property">jM</span>)](<span class="hljs-title function_">oV</span>(gD.<span class="hljs-property">jE</span>));   <span class="hljs-keyword">continue</span>;<br>    ...<br>  &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>特点：</p><ol><li><strong>字符串数组 + 解码函数</strong>：所有字符串都被 <code>qY</code> 或 <code>oV</code> 这类函数包住，真实字符串在一个 ~1400 个元素的数组里（在文件末尾的 <code>function j(){...}</code> 里）</li><li><strong>参数字典</strong>：每个函数开头把一堆局部变量赋成一个”key → index”的对象，调用处写 <code>obj[f(dict.p)]</code> 这种形态</li><li><strong>控制流平坦化</strong>：原本的 <code>if-else</code> 全改成 <code>switch(state)</code> 循环</li><li><strong>垃圾代码注入</strong>：大量 <code>function(){return a^b}</code> 这种只做位运算的 helper，夹在真实逻辑里干扰阅读</li></ol><p>解法分四步，都用 <code>@babel/parser</code> + <code>@babel/traverse</code>：</p><h3 id="第一步：把字符串数组解出来"><a href="#第一步：把字符串数组解出来" class="headerlink" title="第一步：把字符串数组解出来"></a>第一步：把字符串数组解出来</h3><p>先定位那个巨大的字符串数组源头（通常是 <code>function j(){ return &#39;...!...!...&#39;.split(&#39;!&#39;) }</code>），把数组内容 dump 下来。然后写 visitor 替换所有 <code>qY(N)</code> 调用：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// babel plugin（示意，非实际代码）</span><br><span class="hljs-title class_">CallExpression</span>(path) &#123;<br>  <span class="hljs-keyword">if</span> (path.<span class="hljs-property">node</span>.<span class="hljs-property">callee</span>.<span class="hljs-property">name</span> === <span class="hljs-string">&#x27;qY&#x27;</span><br>   &amp;&amp; path.<span class="hljs-property">node</span>.<span class="hljs-property">arguments</span>.<span class="hljs-property">length</span> === <span class="hljs-number">1</span><br>   &amp;&amp; t.<span class="hljs-title function_">isNumericLiteral</span>(path.<span class="hljs-property">node</span>.<span class="hljs-property">arguments</span>[<span class="hljs-number">0</span>])) &#123;<br>    <span class="hljs-keyword">const</span> idx = path.<span class="hljs-property">node</span>.<span class="hljs-property">arguments</span>[<span class="hljs-number">0</span>].<span class="hljs-property">value</span>;<br>    <span class="hljs-keyword">const</span> raw = strings[idx - <span class="hljs-variable constant_">OFFSET</span>];  <span class="hljs-comment">// Cloudflare 会做一次 rotate + XOR</span><br>    <span class="hljs-keyword">if</span> (raw) path.<span class="hljs-title function_">replaceWith</span>(t.<span class="hljs-title function_">stringLiteral</span>(raw));<br>  &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>OFFSET 和 XOR 常数从 VM 初始化代码里静态算出来。这一步跑完，<code>oV(gD.p)</code> 会变成 <code>oV(&#39;someRealString&#39;)</code>，再加一次内联替换就完全去壳了。</p><h3 id="第二步：展平-switch-dispatcher"><a href="#第二步：展平-switch-dispatcher" class="headerlink" title="第二步：展平 switch-dispatcher"></a>第二步：展平 switch-dispatcher</h3><p>控制流平坦化的核心是一个形如 <code>while(true) switch(state) { case &#39;0&#39;:...case &#39;1&#39;:... }</code> 的大循环，外加一个 shuffled 过的派发顺序（代码里的 <code>jd=0</code> 配合 <code>jE[jd++]</code>，<code>jE</code> 是像 <code>&#39;3|0|5|2|1|4&#39;.split(&#39;|&#39;)</code> 这样的数组，决定真实执行顺序）。</p><p>展开它的 babel plugin 逻辑：</p><ol><li>找到 <code>jE = &#39;数字|数字|...&#39;.split(&#39;|&#39;)</code> 这条赋值，拿到执行序列</li><li>按序列顺序把 case 块取出来</li><li>把每个 case 内部的 <code>continue</code> 改成顺序执行</li><li>整个 <code>while + switch</code> 替换成平铺的语句块</li></ol><p>展开完一看，原来的逻辑就露出来了——这个 <code>zq</code> 函数其实只是在做”设置若干 cookie、删除若干属性、然后调用 <code>z6()</code>“这种线性操作。</p><h3 id="第三步：变量重命名"><a href="#第三步：变量重命名" class="headerlink" title="第三步：变量重命名"></a>第三步：变量重命名</h3><p>经过前两步，代码的语义密度已经恢复。接下来用 scope 分析配合启发式命名：</p><ul><li><code>fetch</code> 或 <code>new XMLHttpRequest()</code> 的返回值绑定的变量 → <code>xhr</code> &#x2F; <code>response</code></li><li>参数位上有 <code>body: new Uint8Array(...)</code> 的 → <code>flowPayload</code></li><li>赋给 <code>document.cookie = ...</code> 的右值 → <code>cookieStr</code></li></ul><p>这一步不追求 100% 准确，只要能让读的人判断”哦这段是在发 flow 请求”就够了。</p><h3 id="第四步：摘-API-形态"><a href="#第四步：摘-API-形态" class="headerlink" title="第四步：摘 API 形态"></a>第四步：摘 API 形态</h3><p>走到这一步才开始真正读逻辑。重点锁定三个位置：</p><table><thead><tr><th>位置</th><th>目标</th></tr></thead><tbody><tr><td><code>new XMLHttpRequest().open(&#39;POST&#39;, url)</code> 的调用处</td><td>拿到 flow &#x2F; cv 的 URL 模板</td></tr><tr><td>紧接着的 <code>xhr.send(body)</code></td><td>看 body 怎么拼出来的</td></tr><tr><td><code>_cf_chl_opt</code> 的写入点</td><td>找到 ray、chlVersion、flowSeed 是如何从 response 回填的</td></tr></tbody></table><p><code>diff-flow-artifacts.mjs</code> 和 <code>extract-missing-methods.mjs</code> 做的就是把每次 replay 的这些锚点 dump 出来做 diff，挑缺的环境属性往下一轮补。</p><h2 id="Node-里”补环境”的工程"><a href="#Node-里”补环境”的工程" class="headerlink" title="Node 里”补环境”的工程"></a>Node 里”补环境”的工程</h2><p>读懂接口只是前 30%。真正的重头戏是在 Node 里造一套足够像真浏览器的环境让 challenge VM 能跑完。</p><h3 id="最外层：vm-context"><a href="#最外层：vm-context" class="headerlink" title="最外层：vm context"></a>最外层：vm context</h3><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">const</span> ctx = vm.<span class="hljs-title function_">createContext</span>(&#123;&#125;);<br><span class="hljs-comment">// 注入合成 window / document / navigator / screen</span><br>ctx.<span class="hljs-property">window</span> = ctx;  <span class="hljs-comment">// 自引用</span><br>ctx.<span class="hljs-property">document</span> = <span class="hljs-title function_">buildDocumentStub</span>();<br>ctx.<span class="hljs-property">navigator</span> = <span class="hljs-title function_">buildNavigatorStub</span>(browserCapture.<span class="hljs-property">navigator</span>);<br>ctx.<span class="hljs-property">screen</span> = <span class="hljs-title function_">buildScreenStub</span>(browserCapture.<span class="hljs-property">screen</span>);<br>ctx.<span class="hljs-property">performance</span> = <span class="hljs-title function_">buildPerformanceStub</span>();<br></code></pre></td></tr></table></figure><p><code>browserCapture</code> 是我之前在真 Chrome 里用注入脚本抓的快照——<code>navigator</code> &#x2F; <code>screen</code> 的所有属性、<code>document</code> 里一批稳定 id 元素的几何尺寸、首屏的事件序列，全部一次性导出存成 JSON。</p><h3 id="EventTarget-PointerEvent-这些-Node-默认没有"><a href="#EventTarget-PointerEvent-这些-Node-默认没有" class="headerlink" title="EventTarget &#x2F; PointerEvent 这些 Node 默认没有"></a>EventTarget &#x2F; PointerEvent 这些 Node 默认没有</h3><p>Node 虽然有 <code>EventTarget</code> 的部分实现，但没有 <code>PointerEvent / MouseEvent / TouchEvent</code> 这几个子类，challenge VM 一调就报 <code>ReferenceError</code>。必须手写一遍：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">class</span> <span class="hljs-title class_">EnvEventBase</span> &#123;<br>  <span class="hljs-title function_">constructor</span>(<span class="hljs-params">type, init = &#123;&#125;</span>) &#123;<br>    <span class="hljs-variable language_">this</span>.<span class="hljs-property">type</span> = <span class="hljs-title class_">String</span>(type || <span class="hljs-string">&#x27;&#x27;</span>);<br>    <span class="hljs-variable language_">this</span>.<span class="hljs-property">bubbles</span> = <span class="hljs-title class_">Boolean</span>(init.<span class="hljs-property">bubbles</span>);<br>    <span class="hljs-variable language_">this</span>.<span class="hljs-property">cancelable</span> = <span class="hljs-title class_">Boolean</span>(init.<span class="hljs-property">cancelable</span>);<br>    <span class="hljs-variable language_">this</span>.<span class="hljs-property">isTrusted</span> = init.<span class="hljs-property">isTrusted</span> !== <span class="hljs-literal">false</span>;  <span class="hljs-comment">// 默认 true，对方会读</span><br>    <span class="hljs-variable language_">this</span>.<span class="hljs-property">timeStamp</span> = <span class="hljs-keyword">typeof</span> init.<span class="hljs-property">timeStamp</span> === <span class="hljs-string">&#x27;number&#x27;</span> ? init.<span class="hljs-property">timeStamp</span> : <span class="hljs-number">0</span>;<br>    <span class="hljs-comment">// ...</span><br>  &#125;<br>  <span class="hljs-title function_">preventDefault</span>(<span class="hljs-params"></span>) &#123; <span class="hljs-keyword">if</span> (<span class="hljs-variable language_">this</span>.<span class="hljs-property">cancelable</span>) <span class="hljs-variable language_">this</span>.<span class="hljs-property">defaultPrevented</span> = <span class="hljs-literal">true</span>; &#125;<br>  <span class="hljs-title function_">stopPropagation</span>(<span class="hljs-params"></span>) &#123; <span class="hljs-variable language_">this</span>.<span class="hljs-property">cancelBubble</span> = <span class="hljs-literal">true</span>; &#125;<br>  <span class="hljs-title function_">composedPath</span>(<span class="hljs-params"></span>) &#123; <span class="hljs-keyword">return</span> <span class="hljs-variable language_">this</span>.<span class="hljs-property">target</span> ? [<span class="hljs-variable language_">this</span>.<span class="hljs-property">target</span>] : []; &#125;<br>&#125;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">EnvPointerEvent</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_ inherited__">EnvUIEvent</span> &#123;<br>  <span class="hljs-title function_">constructor</span>(<span class="hljs-params">type, init = &#123;&#125;</span>) &#123;<br>    <span class="hljs-variable language_">super</span>(type, init);<br>    <span class="hljs-variable language_">this</span>.<span class="hljs-property">pointerId</span> = init.<span class="hljs-property">pointerId</span> ?? <span class="hljs-number">1</span>;<br>    <span class="hljs-variable language_">this</span>.<span class="hljs-property">pointerType</span> = init.<span class="hljs-property">pointerType</span> || <span class="hljs-string">&#x27;mouse&#x27;</span>;<br>    <span class="hljs-variable language_">this</span>.<span class="hljs-property">pressure</span> = init.<span class="hljs-property">pressure</span> ?? <span class="hljs-number">0.5</span>;<br>    <span class="hljs-variable language_">this</span>.<span class="hljs-property">tiltX</span> = init.<span class="hljs-property">tiltX</span> ?? <span class="hljs-number">0</span>;<br>    <span class="hljs-comment">// ...</span><br>  &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>这部分没什么技术含量，纯苦力活。坑在对方会读一些冷门属性——<code>twist</code>、<code>tangentialPressure</code>、<code>isPrimary</code>——漏一个就被标记。</p><h3 id="鼠标轨迹合成"><a href="#鼠标轨迹合成" class="headerlink" title="鼠标轨迹合成"></a>鼠标轨迹合成</h3><p>flow 上报里最关键的是事件序列。直接构造一串 <code>{ x: 100, y: 100 }</code>、<code>{ x: 150, y: 100 }</code> 这种线性点位一眼假。用三阶贝塞尔配合可复现的 PRNG：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">function</span> <span class="hljs-title function_">mulberry32</span>(<span class="hljs-params">seed</span>) &#123;<br>  <span class="hljs-keyword">let</span> s = seed | <span class="hljs-number">0</span>;<br>  <span class="hljs-keyword">return</span> <span class="hljs-keyword">function</span>(<span class="hljs-params"></span>) &#123;<br>    s = (s + <span class="hljs-number">0x6D2B79F5</span>) | <span class="hljs-number">0</span>;<br>    <span class="hljs-keyword">let</span> t = s;<br>    t = <span class="hljs-title class_">Math</span>.<span class="hljs-title function_">imul</span>(t ^ (t &gt;&gt;&gt; <span class="hljs-number">15</span>), t | <span class="hljs-number">1</span>);<br>    t ^= t + <span class="hljs-title class_">Math</span>.<span class="hljs-title function_">imul</span>(t ^ (t &gt;&gt;&gt; <span class="hljs-number">7</span>), t | <span class="hljs-number">61</span>);<br>    <span class="hljs-keyword">return</span> ((t ^ (t &gt;&gt;&gt; <span class="hljs-number">14</span>)) &gt;&gt;&gt; <span class="hljs-number">0</span>) / <span class="hljs-number">4294967296</span>;<br>  &#125;;<br>&#125;<br><br><span class="hljs-keyword">function</span> <span class="hljs-title function_">humanTrack</span>(<span class="hljs-params">x0, y0, x1, y1, seed</span>) &#123;<br>  <span class="hljs-keyword">const</span> rand = <span class="hljs-title function_">mulberry32</span>(seed);<br>  <span class="hljs-keyword">const</span> cx1 = x0 + (x1 - x0) * (<span class="hljs-number">0.25</span> + <span class="hljs-title function_">rand</span>() * <span class="hljs-number">0.2</span>);<br>  <span class="hljs-keyword">const</span> cy1 = y0 + (y1 - y0) * <span class="hljs-title function_">rand</span>();<br>  <span class="hljs-keyword">const</span> cx2 = x0 + (x1 - x0) * (<span class="hljs-number">0.55</span> + <span class="hljs-title function_">rand</span>() * <span class="hljs-number">0.2</span>);<br>  <span class="hljs-keyword">const</span> cy2 = y0 + (y1 - y0) * (<span class="hljs-number">0.5</span> + <span class="hljs-title function_">rand</span>() * <span class="hljs-number">0.5</span>);<br>  <span class="hljs-keyword">const</span> steps = <span class="hljs-number">24</span> + <span class="hljs-title class_">Math</span>.<span class="hljs-title function_">floor</span>(<span class="hljs-title function_">rand</span>() * <span class="hljs-number">12</span>);<br>  <span class="hljs-keyword">const</span> points = [];<br>  <span class="hljs-keyword">let</span> t = <span class="hljs-number">0</span>;<br>  <span class="hljs-keyword">for</span> (<span class="hljs-keyword">let</span> i = <span class="hljs-number">0</span>; i &lt;= steps; i++) &#123;<br>    t = i / steps;<br>    <span class="hljs-comment">// Bezier + per-axis jitter</span><br>    <span class="hljs-keyword">const</span> x = <span class="hljs-title function_">bezier</span>(x0, cx1, cx2, x1, t) + (<span class="hljs-title function_">rand</span>() - <span class="hljs-number">0.5</span>) * <span class="hljs-number">2</span>;<br>    <span class="hljs-keyword">const</span> y = <span class="hljs-title function_">bezier</span>(y0, cy1, cy2, y1, t) + (<span class="hljs-title function_">rand</span>() - <span class="hljs-number">0.5</span>) * <span class="hljs-number">2</span>;<br>    points.<span class="hljs-title function_">push</span>(&#123; x, y, <span class="hljs-attr">dt</span>: <span class="hljs-number">8</span> + <span class="hljs-title class_">Math</span>.<span class="hljs-title function_">floor</span>(<span class="hljs-title function_">rand</span>() * <span class="hljs-number">10</span>) &#125;);  <span class="hljs-comment">// 8~17ms 一个点</span><br>  &#125;<br>  <span class="hljs-keyword">return</span> points;<br>&#125;<br></code></pre></td></tr></table></figure><p>生成的轨迹会被再包一层 <code>PointerEvent</code>，按每个点的 <code>dt</code> 设 <code>timeStamp</code>，一次性 dispatch 给 widget 容器。可复现这一点在调试时很有用——同样的 seed 出同样的轨迹，出错了方便 diff。</p><h3 id="缺什么补什么"><a href="#缺什么补什么" class="headerlink" title="缺什么补什么"></a>缺什么补什么</h3><p>每次跑 VM 都会有新的报错，主要两类：</p><ul><li><code>TypeError: X is not a function</code> → 说明 VM 调用了某个我没实现的方法</li><li><code>TypeError: Cannot read properties of undefined (reading &#39;Y&#39;)</code> → 说明 VM 访问了某个我没实现的属性</li></ul><p>写了两个扫描脚本，跑完一轮 replay 后自动扫出 top N 的 missing，下一轮开跑前统一补进 stub：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// extract-missing-methods.mjs 的核心</span><br><span class="hljs-keyword">function</span> <span class="hljs-title function_">scan</span>(<span class="hljs-params">logs</span>) &#123;<br>  <span class="hljs-keyword">const</span> missing = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Map</span>();<br>  <span class="hljs-keyword">for</span> (<span class="hljs-keyword">const</span> err <span class="hljs-keyword">of</span> logs.<span class="hljs-title function_">filter</span>(<span class="hljs-function"><span class="hljs-params">l</span> =&gt;</span> l.<span class="hljs-property">type</span> === <span class="hljs-string">&#x27;TypeError&#x27;</span>)) &#123;<br>    <span class="hljs-keyword">const</span> m = err.<span class="hljs-property">message</span>.<span class="hljs-title function_">match</span>(<span class="hljs-regexp">/(\w+)\.(\w+) is not a function/</span>);<br>    <span class="hljs-keyword">if</span> (!m) <span class="hljs-keyword">continue</span>;<br>    <span class="hljs-keyword">const</span> key = <span class="hljs-string">`<span class="hljs-subst">$&#123;m[<span class="hljs-number">1</span>]&#125;</span>.<span class="hljs-subst">$&#123;m[<span class="hljs-number">2</span>]&#125;</span>`</span>;<br>    missing.<span class="hljs-title function_">set</span>(key, (missing.<span class="hljs-title function_">get</span>(key) || <span class="hljs-number">0</span>) + <span class="hljs-number">1</span>);<br>  &#125;<br>  <span class="hljs-keyword">return</span> [...missing.<span class="hljs-title function_">entries</span>()].<span class="hljs-title function_">sort</span>(<span class="hljs-function">(<span class="hljs-params">a, b</span>) =&gt;</span> b[<span class="hljs-number">1</span>] - a[<span class="hljs-number">1</span>]);<br>&#125;<br></code></pre></td></tr></table></figure><p>这一步效率很高。前三四轮补得很快，<code>Intl.Segmenter</code>、<code>ReadableStream.pipeTo</code>、<code>crypto.subtle.digest</code> 这些都补齐之后，VM 能跑到 flow 上报了。</p><h2 id="为什么还是没过"><a href="#为什么还是没过" class="headerlink" title="为什么还是没过"></a>为什么还是没过</h2><p>单说”跑到 flow 能上报”是够呛的表面胜利。真正的问题在后面。</p><h3 id="一、flow-上报通过不了服务端校验"><a href="#一、flow-上报通过不了服务端校验" class="headerlink" title="一、flow 上报通过不了服务端校验"></a>一、flow 上报通过不了服务端校验</h3><p>flow 的 body 不只是把环境字段 JSON 序列化那么简单。encrypted body 用的密钥派生方式是 <code>SHA-256(cRay + flowSeed + counter)</code> 然后 AES-CTR，而<strong>加密的 payload 里还混入了 challenge VM 自己的”执行指纹”</strong>——比如它在 VM 里调了 <code>Object.getOwnPropertyNames(window)</code>，预期结果是一个特定的名字排列，这个排列会被揉进加密输入。</p><p>我的 <code>window</code> stub 虽然属性齐全，但 <strong>V8 的属性枚举顺序</strong>（涉及 numeric key 先枚举、然后是 string key 的插入顺序）不完全等价于真 Chrome 的 <code>window</code>，因为我是一层一层往 ctx 上 <code>defineProperty</code> 加的，顺序跟真 Chrome 由 IDL 生成的顺序不同。对方在 encrypted payload 里夹了这个枚举结果的 hash。</p><p>想绕过这条只有一条路：<strong>把 stub 换成从真 Chrome dump 的 proto chain 原样重放</strong>。但这样又引入另一个问题——它怎么可能和一台真浏览器产生的打点<strong>在时序上</strong>完全一致？flow 里含 <code>performance.now()</code> 的读数分布，我的代码跑得再慢都不像人手。</p><h3 id="二、challenge-VM-自带完整性检查"><a href="#二、challenge-VM-自带完整性检查" class="headerlink" title="二、challenge VM 自带完整性检查"></a>二、challenge VM 自带完整性检查</h3><p>VM 会反复读几样东西：</p><ul><li><code>Function.prototype.toString</code> 的返回值里是否包含 <code>[native code]</code>（Proxy 代理过的函数这里会露馅）</li><li><code>Error().stack</code> 的格式（Node 的 stack 格式和 V8 浏览器上下文的格式略有不同）</li><li><code>performance.now()</code> 的抖动分布（真浏览器是亚毫秒，Node 里用 <code>performance.now()</code> 更干净，反而像假的）</li><li><code>document.createElement(&#39;canvas&#39;).getContext(&#39;2d&#39;).measureText(...)</code> 的返回（跟渲染引擎强绑定，我没法实现）</li></ul><p>想补齐 canvas &#x2F; WebGL &#x2F; AudioContext 就是在 Node 里重做半个浏览器。开源方案有 <code>jsdom</code> + <code>canvas</code> + <code>headless-gl</code>，但它们产生的指纹和真 Chrome 不一致，等于换了个靶子给对方打。</p><h3 id="三、每周更新"><a href="#三、每周更新" class="headerlink" title="三、每周更新"></a>三、每周更新</h3><p>抓了两次 orchestrate 快照，文件 hash 一次是 <code>9f7a3617</code>，一周后变成 <code>9f81c061</code>。diff 一下发现：</p><ul><li>字符串数组的 OFFSET 变了</li><li>switch dispatcher 的执行序列打乱方式换了一种</li><li>新增了两个从前没见过的”环境探针”（读 <code>navigator.connection.rtt</code>、<code>WebAssembly.instantiate</code> 的 hook）</li></ul><p>这意味着 AST 工具链里所有跟数组索引相关的 magic number 全要重算。一次迭代我要花半天到一天，Cloudflare 那边估计是自动发布。算力不对等，一个人跟不动。</p><h3 id="四、最后那个路线也塌了"><a href="#四、最后那个路线也塌了" class="headerlink" title="四、最后那个路线也塌了"></a>四、最后那个路线也塌了</h3><p>既然 VM 过不了，退一步——<strong>不自己解 Turnstile，外接 solver</strong>。<code>turnstile-solver.mjs</code> 就是这条兜底路线：向 2captcha &#x2F; capsolver &#x2F; 自建 CDP 节点提交 sitekey + 页面 URL，由它们用真浏览器解出 token，我只负责拿着 token 去 POST <code>cv/result</code>。</p><p>这条路技术上通——我跑通过几次，但它的问题是：</p><ul><li><strong>没解决最初想解决的问题</strong>。我一开始是为了不启动浏览器，结果解成依赖别人的浏览器</li><li><strong>token 和 session 强绑定</strong>。拿 solver 提交的 token 必须来自同一 IP、同一 UA 的 session，外接 solver 意味着要把整个 session 透传给它们，或者它们解完我再带着 token 回来——后者会因为时间差被 cv&#x2F;result 拒掉</li><li><strong>成本</strong>。商用 solver 按次计费，自建 CDP 节点又回到”为什么不直接用 Playwright”的问题</li></ul><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>做完这个项目，我对 Cloudflare Managed Challenge 的看法是：</p><ul><li><strong>单点接口（orchestrate &#x2F; flow &#x2F; cv）形态是可以逆出来的</strong>，AST 工具链足够处理它的混淆层</li><li><strong>challenge VM 的完整性校验 + 加密 payload 的执行指纹绑定</strong>，这两层组合起来让”在 Node 里补环境”成为近乎不可能的任务，除非你有团队 + 真浏览器级别的原型基线</li><li><strong>正确的工程选择是尊重它</strong>：如果你在做合法的可用性监控，直接用 Playwright 带 <code>storageState</code> 复用；如果你是站点方，把 5s 盾当成已经足够好的默认方案，不用焦虑有人能”破解”它</li></ul><p>这个仓库我封存了，当做一次 AST + 补环境 + TLS 指纹的综合训练。顺手收获的几样东西——<code>node-tls-client</code> 的用法、<code>@babel/parser</code> 写 visitor 的手感、Node VM context 隔离的边界——之后会长期用得上。</p><hr><blockquote><p>本文不含任何可执行的绕过实现。所有技术点（AST、TLS 指纹、事件补全、鼠标轨迹生成）都适用于合法场景：E2E 测试、SDK 审计、自家站点监控、前端混淆代码的逆向分析。</p></blockquote>]]>
    </content>
    <id>https://7788dev.github.io/2026/05/08/turnstile-failed-recap/</id>
    <link href="https://7788dev.github.io/2026/05/08/turnstile-failed-recap/"/>
    <published>2026-05-08T13:45:00.000Z</published>
    <summary>在 Node 里补全 Cloudflare Managed Challenge（俗称 5s 盾）运行所需的 DOM / 事件 / 定时器环境，逆向 orchestrate、flow、cv/result 三条接口，最终卡在哪里。</summary>
    <title>逆向 Cloudflare 5s 盾补环境</title>
    <updated>2026-05-08T14:05:31.117Z</updated>
  </entry>
  <entry>
    <author>
      <name>Looks</name>
    </author>
    <content>
      <![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p><h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo new <span class="hljs-string">&quot;My New Post&quot;</span><br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p><h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo server<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p><h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo generate<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p><h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo deploy<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>]]>
    </content>
    <id>https://7788dev.github.io/2026/05/08/hello-world/</id>
    <link href="https://7788dev.github.io/2026/05/08/hello-world/"/>
    <published>2026-05-08T13:33:51.050Z</published>
    <summary>
      <![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a>]]>
    </summary>
    <title>Hello World</title>
    <updated>2026-05-08T13:33:51.050Z</updated>
  </entry>
</feed>
